<li>
<span><%= link_to micropost.user.name, micropost.user %></span>
<span><%= micropost.content %></span>

<% if current_user?(micropost.user) %>
  <span>
  <%= link_to "削除", micropost, method: :delete, data: {confirm: "本当に削除しますか？"} %>
  </span>
<% end %>

<p><%= time_ago_in_words(micropost.created_at) %>前に投稿されました。</p>
</li>
